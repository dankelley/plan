\name{trim.whitespace}

\alias{trim.whitespace}

\title{Trim leading/trailing whitespace from character strings}

\description{Trim leading and trailing whitespace from character
  strings.  Used by \code{\link{read.gantt}} and
  \code{\link{read.burndown}}.}

\usage{trim.whitespace(x)}

\arguments{
  \item{x}{a character string, or vector of character strings.}
}

\value{As \code{x}, but with leading and trailing space removed}

\examples{
library(plan)
x <- c("  hellow there", "ba bye   ", " buddy   ")
print(x)
print(trim.whitespace(x))
}

\author{Dan Kelley}

\keyword{misc}
